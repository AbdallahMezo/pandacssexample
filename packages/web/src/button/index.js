import { ark } from "@ark-ui/react/factory";
import { styled } from "../../styled-system/jsx";
import { appButton } from "../../styled-system/recipes";

export const ButtonFromConfigRecipe = styled(ark.button, appButton);
